function processEmployee() {
    // create HTML to create a new table row
    // within the row, create columns for each data point of an employee

    existingTable = document.getElementById();
    document.createElement();
}